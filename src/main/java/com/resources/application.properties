# Application Properties

# Server Configuration
server.port=8080
server.host=localhost

# Database Configuration
db.url=jdbc:mysql://localhost:3306/strathosnet
db.username=root
db.password=yourpassword

# API Configuration
api.base.url=https://api.strathosnet.com
api.key=test_api_key

# Logging Configuration
logging.level.root=INFO
logging.level.com.strathosnet=DEBUG

# Other Application Settings
app.name=StrathosNet
app.version=1.0.0
